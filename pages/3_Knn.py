import streamlit as st

st.title("K-Nearest Neighbors")
st.write("The k-nearest neighbors (KNN) algorithm is a non-parametric, supervised learning classifier, \
         which uses proximity to make classifications or predictions about the grouping of an individual \
         data point. It is one of the popular and simplest classification and regression classifiers used \
         in machine learning today.[11] Below are the reults we got from the KNN model.")
st.image("Media/knn_report.png", width=450)
st.image("Media/knn_cm.png", width=450)